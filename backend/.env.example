# Database - Docker PostgreSQL
# IMPORTANTE: Cambia estas credenciales en producción
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tienda_ecommerce"

# App Configuration
NODE_ENV=development
PORT=3001

# JWT Configuration
# IMPORTANTE: Genera secrets seguros para producción
# Puedes usar: openssl rand -base64 32
JWT_SECRET=tu-secreto-super-seguro-cambia-esto-en-produccion
JWT_REFRESH_SECRET=tu-secreto-refresh-super-seguro-cambia-esto

# CORS (Frontend URL)
FRONTEND_URL=http://localhost:3000

# Security
# Rate Limiting (requests por minuto)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Optional: Cloudinary (para imágenes)
# CLOUDINARY_CLOUD_NAME=your_cloud_name
# CLOUDINARY_API_KEY=your_api_key
# CLOUDINARY_API_SECRET=your_api_secret

# Optional: Email Service
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password
